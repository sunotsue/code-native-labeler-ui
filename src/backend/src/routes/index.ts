/**
 * Barrel export for all routes
 * Import this in server.ts to mount all routes at once
 */

export { taskRoutes } from './tasks';
export { testRoutes } from './tests';
export { labelRoutes } from './labels';
